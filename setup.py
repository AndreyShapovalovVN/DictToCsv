#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='DictToCSV',
    version='0.0.1',
    description='Convert Dict to CSV',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
